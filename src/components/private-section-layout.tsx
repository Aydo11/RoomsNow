export const privateSectionMetadata = {
  robots: { index: false, follow: false, noarchive: true, nosnippet: true },
};

export function PrivateSectionLayout({ children }: { children: React.ReactNode }) {
  return children;
}
